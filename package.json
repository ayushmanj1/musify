{
  "name": "musify",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "vercel-build": "cd frontend && npm install --legacy-peer-deps && npm run build",
    "start": "node --max-old-space-size=256 backend/server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "lru-cache": "^11.0.0",
    "youtube-dl-exec": "^3.1.5",
    "youtube-sr": "^4.3.11",
    "youtubei.js": "^17.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
